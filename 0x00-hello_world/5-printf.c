#include <stdio.h>

/**
 * main - Entry point of a program
 * Description: A program that prints to stdout using printf function
 * Return: Always(success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art");
	return (0);
}
